/* import { useState } from "react";

const ViewAll = ({ blogs }) => {
    const [show, setShow] = useState(false)

    const hideWhenVisible = { display: show ? '' :  }


}

export default ViewAll
 */